import React from "react";
import { Outlet } from "react-router-dom";

const CarbonCreditAssetsFromRegistry: React.FC = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default CarbonCreditAssetsFromRegistry;
